"use client";
import { Box, Button } from "@frenchies-spots/material";
import React from "react";

interface PageProps {}

const Page = (props: PageProps) => {
  return (
    <Box>
      <Button>TEST</Button>
    </Box>
  );
};

export default Page;
