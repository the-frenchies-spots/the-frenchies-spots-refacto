import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  paginationBar: {
    display: "flex",
    flexDirection: "row",
    flexGrow: 0,
    flexShrink: 1,
    flexBasis: "auto",
    paddingHorizontal: 10,
  },
});
