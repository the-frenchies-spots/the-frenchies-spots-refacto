import { StyleSheet } from "react-native";
import { theme } from "@frenchies-spots/theme";

const { darkGrey } = theme.TFS.colors;

export const styles = StyleSheet.create({
  caption: {
    fontFamily: "Montserrat-Medium",
    fontStyle: "normal",
    fontWeight: "500",
    fontSize: 10,
  },
});
